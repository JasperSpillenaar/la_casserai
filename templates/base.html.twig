<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom box-shadow">
            <h5 class="my-0 mr-md-auto font-weight-normal"><a href="/" style="text-decoration: none; color: black;">Hotel La Casserai</a></h5>
            <nav class="my-2 my-md-0 mr-md-3">
                {% if is_granted('ROLE_ADMIN') %}
                    <a class="p-2 text-dark" href="/room">Room</a>
                    <a class="p-2 text-dark" href="/reservation">Reservation</a>
                    <a class="btn btn-outline-primary" href="/logout">Logout</a>
                {% else %}
                    {%if is_granted('ROLE_USER') %}
                        <a class="btn btn-outline-primary" href="/logout">Logout</a>
                    {% else %}
                        <a class="btn btn-outline-primary" href="/register">Sign up</a>
                        <a class="btn btn-outline-primary" href="/login">Login</a>
                    {% endif %}
                {% endif %}

            </nav>

        </div>
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
